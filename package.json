{
  "name": "slate",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "start": "cd app && npm start",
    "lint": "cd app && npm run lint",
    "package": "cd app && npm run package",
    "make": "cd app && npm run make",
    "publish": "cd app && npm run publish",
    "install": "cd app && npm install",
    "test": "cd app && npm test",
    "ci": "cd app && npm run ci"
  }
}
